# Blondie Secrets Template - COPY TO secrets.env.yaml and fill values
# Mount at runtime: docker run -v ./secrets.env.yaml:/workspace/.agent/secrets.env.yaml
# Never commit real values - add to .gitignore

llm:
  openai:
    api_key: "sk-proj-..."
  anthropic:
    api_key: "claude-..."
git:
  github_token: "ghp_..."
